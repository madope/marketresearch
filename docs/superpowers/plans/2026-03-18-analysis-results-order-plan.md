# Analysis Results Order Plan

1. Update `frontend/src/features/research/dashboard.tsx`
   - Swap the render order of the `市场分析` and `价格表格` sections.

2. Verify
   - Run `npm test -- --run tests/app.test.tsx`.
